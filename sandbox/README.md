# Agent Sandbox

This directory is the only local filesystem area exposed to the `file_read`
tool. Add UTF-8 text files here when testing the `workspace-inspection` Skill.

Absolute paths and paths escaping this directory are rejected.
